# Legacy Implementations

At the beginning of the project, the crawler was implemented in Go and the search engine was implemented in Rust.

Due to the project requirements, both components were later ported to Python. The current Python implementations live in `crawl/` and `search/`.
