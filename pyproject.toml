[tool.uv.workspace]
members = [
    "crawl",
    "search",
    "verdict-ml",
    "benchmark/crawl",
    "benchmark/index_storage",
    "benchmark/retrieval",
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
pythonpath = ["search/tests"]
